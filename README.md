# rust 实现显示文件树命令行

```shell
$ rust_tree -h
tree 0.1.0
Tan <liangzhong.tan@outlook.com>

USAGE:
    rust_tree [OPTIONS]

OPTIONS:
    -a, --all        all
    -h, --help       Print help information
    -l, --level      tree level
    -p, --path       path
    -V, --version    Print version information
```




